__pycache__/
*.pyc
.venv/
.idea/
.pytest_cache/
.mypy_cache/
.ruff_cache/
dist/
*.egg-info/
.coverage
volume/
OLD_HARVESTER.md
PLAN.md
# Terraform (ops/terraform) — .terraform.lock.hcl is deliberately NOT
# ignored: commit the one your own `terraform init` generates.
.terraform/
*.tfstate
*.tfstate.*
crash.log
terraform.tfvars
*.auto.tfvars
